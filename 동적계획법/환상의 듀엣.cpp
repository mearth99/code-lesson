#include <iostream>
#include <algorithm>
using namespace std;

int arr[2001];
int DP[2001][2001];
int N,minv = INT32_MAX;
int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
    cin >> N;
    for(int i=0;i<N;i++)
        cin >> arr[i];
	for(int i=0;i<N;i++){

    }
}