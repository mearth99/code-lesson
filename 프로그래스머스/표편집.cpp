#include <string>
#include <vector>

using namespace std;

string solution(int n, int k, vector<string> cmd) {
    vector<bool> table(n);
    int idx=0;
    string answer = "";
    for(auto text:cmd){
        if(text.at(0)=='C')
        if(text.at(0)=='Z')
    }
    return answer;
}