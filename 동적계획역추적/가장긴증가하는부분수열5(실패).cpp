#include<algorithm>
#include<iostream>
#include<vector>

using namespace std;

int main(){
    int n,dp_idx=0,it=1;
    cin >> n;
    vector<int> arr(n);
    vector<int> DP(n+1);
    vector<int> sub;

    for(int i=0;i<n;i++)
        cin >> arr[i];
    
    return 0;
}